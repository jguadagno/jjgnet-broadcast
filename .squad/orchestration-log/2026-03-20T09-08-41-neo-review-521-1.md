# Orchestration Log: Neo — PR #521 Review (Null→404 Fix)

**Timestamp:** 2026-03-20T09:08:41Z  
**Agent:** Neo (Lead)  
**Task:** Retroactive review of PR #521 (`squad/519-fix-null-ref-404`) — already merged by jguadagno  
**Requested by:** Coordinator  
**Status:** ✅ COMPLETE — APPROVED (retroactive)

## Context

PR #521 was merged by jguadagno before formal review. Neo conducted a retroactive review to confirm correctness and document findings. Issue #519 was auto-closed by the merge.

## Review Findings

### Production Code Changes (all correct ✅)
- `EngagementsController.GetEngagementAsync` — null guard + `return Ok(ToResponse(engagement))`
- `EngagementsController.GetTalkAsync` — return type fixed (`Task<TalkResponse>` → `Task<ActionResult<TalkResponse>>`), null guard added
- `SchedulesController.GetScheduledItemAsync` — null guard + `return Ok(ToResponse(item))`
- **Bonus:** Scope acceptance updated to include granular scopes (`.List`/`.View`/`.Modify`) alongside `.All` in EngagementsController

### Test Changes (all correct ✅)
- `ThrowsNullReferenceException` tests replaced with `ReturnsNotFound` using `result.Result.Should().BeOfType<NotFoundResult>()`
- Success tests updated: `result.Value` → `result.Result.Should().BeOfType<OkObjectResult>().Subject`

### Minor Gap Flagged ⚠️
- `GetTalkAsync` scope still only accepts `Talks.All` (`.View` remains commented) — pre-existing issue, not introduced by this PR
- Created follow-up **Issue #527**: `fix(api): GetTalkAsync only accepts Talks.All scope — add Talks.View support`

## Patterns Confirmed

1. Null guard before `ToResponse()` is required — never pass null into mapping helpers
2. Explicit `return Ok(value)` requires `OkObjectResult` test pattern, not `result.Value`
3. `ActionResult<T>` return type required for methods that may return `NotFound()`

## Outcome

- PR #521 confirmed correct and safe
- Issue #519 closed (auto-closed by merge)
- Issue #527 created as follow-up scope gap
- Decisions recorded in inbox: `neo-521-merge.md`
