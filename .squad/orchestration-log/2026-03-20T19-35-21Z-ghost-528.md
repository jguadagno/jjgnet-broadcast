# Orchestration Log: Ghost Issue #528 MSAL Fix

**Timestamp:** 2026-03-20T19:35:21Z  
**Agent:** Ghost (Web Auth)  
**Task:** Add [AuthorizeForScopes] to API-calling controllers (Issue #528)  

## Summary

Fixed MSAL token cache eviction handling per issue #528 requirements. Applied `[AuthorizeForScopes]` class-level attribute (no parameters) to all four MVC controllers that call downstream APIs:
- EngagementsController
- TalksController
- SchedulesController
- MessageTemplatesController

This attribute ensures transparent re-authentication when the token cache is evicted or cleared.

**Architecture confirmed:**
- Web project uses SQL-backed distributed token cache (`AddDistributedSqlServerCache` + `AddDistributedTokenCaches()`)
- Token cache table (`dbo.Cache`) persists across restarts
- Two complementary scenarios handled:
  1. Account entirely missing from cache → cookie rejection (existing handler)
  2. Account in cache but API token missing → OIDC challenge via `[AuthorizeForScopes]` (new)

Opened PR #532.

## Outcome

✅ [AuthorizeForScopes] applied to all four Web controllers  
✅ PR #532 opened  
✅ SQL token cache architecture confirmed in place
