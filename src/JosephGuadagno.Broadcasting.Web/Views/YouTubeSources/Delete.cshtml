@model JosephGuadagno.Broadcasting.Web.Models.YouTubeSourceViewModel
@{
    ViewData["Title"] = "Delete YouTube Source";
}

<div class="container">
    <h1 class="display-4 mb-4 text-danger">Delete YouTube Source</h1>
    
    <div class="alert alert-warning" role="alert">
        <i class="bi bi-exclamation-triangle-fill me-2"></i>
        <strong>Warning:</strong> Are you sure you want to delete this YouTube source?
    </div>
    
    <div class="card">
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-3">Title</dt>
                <dd class="col-sm-9">@Model.Title</dd>
                
                <dt class="col-sm-3">Video ID</dt>
                <dd class="col-sm-9">@Model.VideoId</dd>
                
                <dt class="col-sm-3">Author</dt>
                <dd class="col-sm-9">@Model.Author</dd>
                
                <dt class="col-sm-3">Publication Date</dt>
                <dd class="col-sm-9">@Model.PublicationDate.ToString("F")</dd>
            </dl>
        </div>
    </div>
    
    <form asp-action="Delete" method="post" class="mt-3">
        <input type="hidden" asp-for="Id" />
        <button type="submit" class="btn btn-danger">
            <i class="bi bi-trash me-1"></i>Yes, Delete
        </button>
        <a asp-action="Index" class="btn btn-secondary">
            <i class="bi bi-x-circle me-1"></i>Cancel
        </a>
    </form>
</div>
